services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile

    image: frontend
    container_name: frontend
    ports:
      - 8888:8888
    restart: unless-stopped

    volumes:
      - ./frontend:/app
      - /app/node_modules
